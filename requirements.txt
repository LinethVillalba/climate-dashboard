# Core requirements for running the server
# No additional packages are required as we're using Python's built-in http.server
# The web application uses CDN-hosted libraries (Leaflet.js, Chart.js, D3.js) 